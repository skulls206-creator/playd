You are updating my local‑music web player.

Current behavior (what it does now):

There is a search/filter bar that only filters the sidebar lists of artists and albums.

The main window shows tracks based on the selected artist/album, not based directly on the search text.

When I type in the search bar, it does NOT show a list of matching tracks in the main window, and I can’t just click and play the exact file I searched for.

Goal (what I want instead):

Turn the search bar into a GLOBAL track search.

When the user types a query, the MAIN TRACK VIEW should switch to a “Search results” view that lists ALL tracks whose:

filename, OR

track title, OR

artist, OR

album
contains the search text (case‑insensitive).

The resulting list in the main window must be real track rows wired to my existing play logic so clicking a result plays that specific file.

The sidebar artist/album lists can either:

clear selection while searching, or

be filtered consistently with the track results,
but the priority is that the main window always shows the search results while search text is non‑empty.

Implementation details:

I have a tracks array already, shaped roughly like:

ts
type Track = {
  id: string;
  fileName: string;
  title: string;
  artist: string;
  album: string;
  path: string;      // or a FileSystemFileHandle / URL for playback
  duration?: number;
};
I also have:

State for allTracks (full library).

State for filteredArtists / filteredAlbums (sidebar).

A component that renders the main track list (table or list) given an array of Track.

A function like playTrack(track: Track) already working when clicking a track row.

What I need you to do:

Add state for searchQuery and for visibleTracks (the tracks currently shown in the main window).

Wire the search input’s onChange so that:

searchQuery is updated.

If searchQuery is empty:

visibleTracks follows the normal logic (selected artist/album, current playlist, etc.).

If searchQuery is non‑empty:

visibleTracks becomes:

ts
allTracks.filter(t => {
  const q = searchQuery.toLowerCase();
  return (
    t.fileName.toLowerCase().includes(q) ||
    (t.title ?? '').toLowerCase().includes(q) ||
    (t.artist ?? '').toLowerCase().includes(q) ||
    (t.album ?? '').toLowerCase().includes(q)
  );
});
Make the main window always render from visibleTracks instead of re‑deriving the list inside that component.

Ensure each row in the “Search results” view:

Displays at least title, artist, album, and maybe fileName/path.

Calls playTrack(track) on click, like in the regular view.

OPTIONAL, but nice:

Add a small “Search results for: ‘<query>’ (N tracks)” label at the top of the main window when searchQuery is non‑empty.

When the user clears the search box, revert back to the normal artist/album‑based view.

Important constraints:

Do NOT break the existing local file playback logic (I’m using the File System Access API and local File / FileSystemFileHandle objects).

Keep search entirely client‑side over the in‑memory list of tracks that I built after scanning the user’s folder.

Keep the implementation in my existing stack: [describe yours here: React/Vanilla/Next/etc.].

Deliverables:

Updated/added React (or JS/TS) components implementing this behavior.

Exact code for:

The state setup.

The search handler.

The filtering logic.

The main window render mapping over visibleTracks.